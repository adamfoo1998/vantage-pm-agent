---
description: End-of-day wrap — file today's deliverables via librarian, refresh STATUS.md, list rollover.
---

Run Adam's end-of-day wrap. Be terse.

1. **File today's deliverables.** Identify any finished documents produced or discussed this
   session (or sitting unfiled in `inbox/`) that haven't yet been filed to `vault/`. Hand each to
   `librarian` to file with proper frontmatter.

2. **Refresh `STATUS.md`.** Have `librarian` update it: active projects, open action items,
   blocked/waiting-on, last briefing date, remaining unprocessed inbox items — reflecting only
   what actually landed today, not invented status.

3. **Rollover.** List what did NOT get done today and will carry to tomorrow — pull from any
   top-3 set by `/standup` this morning plus anything newly opened today.

Keep the whole response under a screen.
